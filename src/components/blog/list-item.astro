---
import { Icon } from "astro-icon/components";
import { getLocaleUrl, useTranslations } from "@utils/i18n";
import { Image } from 'astro:assets';
import FallbackImage from "@images/content/articles-fallback.jpg";
import type { CollectionEntry } from "astro:content";
import { themeConfig } from '~/theme.config';
import { getReadingTime } from '@utils/reading-time';

interface Props {
  entry: CollectionEntry<'articles'>;
  locale: string;
  wide?: boolean;
	noTransition?: boolean;
}

// define base classes
const baseCardClasses = "group flex h-full rounded-lg no-underline drop-shadow hover:drop-shadow-xl";
const baseImageClasses = "h-full w-full bg-top object-cover";
const baseContentClasses = "border border-l-0 flex h-auto grow flex-col justify-between w-full rounded-r-lg bg-white dark:bg-stone-900 text-text dark:text-text-dark";
const baseTitleClasses = "text-balance font-semibold text-text dark:text-text-dark";

const { entry, locale, wide, noTransition } = Astro.props as Props;
const featured = entry.data.featured || false;

const t = useTranslations(locale);

// define image with potential fallback
const imageFile = entry.data.image?.file ? entry.data.image.file : (!entry.data.image?.url && themeConfig.articles?.imageFallback) ? FallbackImage : null;

---

<div>
  <a href={getLocaleUrl('posts/' + entry.id.split('/').slice(-1)[0], locale)} class:list={[baseCardClasses, !wide && 'sm:flex-col']}>
    <span class:list={['xs:w-36 relative flex h-auto w-28 shrink-0 overflow-clip rounded-l-lg border border-r-0', wide ? 'sm:w-56 md:max-h-60 md:w-80 lg:w-[400px]' : 'sm:h-40 sm:w-full sm:rounded-t-lg sm:rounded-bl-none sm:border-r sm:border-b-0', featured ? 'border-primary' : 'group-hover:border-stone-500 border-stone-400 dark:border-stone-700']}>
      {
        imageFile ? (
          <Image src={imageFile} alt={entry.data.image?.alt ?? ''} class={baseImageClasses} transition:name={!noTransition ? 'image-' + entry.id : ''} draggable="false" priority />
        ) : entry.data.image?.url ? (
          <Image src={entry.data.image.url} alt={entry.data.image?.alt ?? ''} class={baseImageClasses} transition:name={!noTransition ? 'image-' + entry.id : ''} draggable="false" priority inferSize />
        ) : (
          <span class:list={['absolute inset-0 flex items-center justify-center p-8', !wide && themeConfig.articles?.textOverImage && 'sm:hidden']}>
            <Icon name="heroicons:document-text" class="h-12 w-12 opacity-70 lg:h-16 lg:w-16" />
          </span>
        )
      }

      <span
        class:list={[
          'absolute inset-0 flex items-end rounded-l-lg px-3 py-2 transition-colors group-hover:bg-stone-900/10 dark:group-hover:bg-white/10',
          !wide && 'sm:rounded-t-lg sm:rounded-bl-none',
        ]}
      >
        <span class:list={[baseTitleClasses, 'hidden text-lg', !wide && themeConfig.articles?.textOverImage && 'sm:block']}>{entry.data.title}</span>
      </span>
      {featured && <span class="bg-primary text-primary-contrast absolute inset-0 h-fit w-fit rounded-tl-md rounded-br-lg px-2 py-1 text-xs drop-shadow">{t('featured')}</span>}
    </span>

    <span
      class:list={[
        baseContentClasses,
        featured ? 'border-primary' : 'group-hover:border-stone-500 border-stone-400 dark:border-stone-700',
        wide ? 'sm:text-base' : 'sm:rounded-tr-none sm:rounded-b-lg sm:border-t-0 sm:border-l',
        'group-hover:bg-stone-100 dark:group-hover:bg-stone-800 px-5 py-4 text-sm font-normal text-stone-600 transition-colors dark:text-stone-300',
      ]}
    >
      <span class="block">
        <span class:list={[baseTitleClasses, 'xs:text-lg mb-4 block', wide && 'sm:text-xl', !wide && themeConfig.articles?.textOverImage && 'sm:hidden']}>{entry.data.title}</span>
        {entry.data.excerpt}
      </span>
      <span class="hidden w-full justify-between pt-3 pr-1 group-hover:pr-0 sm:flex">
        <div class="flex items-center gap-1 text-xs opacity-80 group-hover:opacity-100" aria-label={getReadingTime(entry.body) + ' ' + t('min-read')}>
          <Icon name="heroicons:clock" class="h-3 w-3" />
          {getReadingTime(entry.body) + ' ' + t('labels.min-read')}
        </div>
        <Icon name="heroicons:arrow-right" class="h-5 w-5" />
      </span>
    </span>
  </a>
</div>
